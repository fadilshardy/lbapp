export interface ICategory {
    id: string;
    name: string;
    description: string;
    parent_id: string;
}

export interface ISelectCategory {
    id: string;
    name: string;
    description: string;
    parent_id: string;
}
